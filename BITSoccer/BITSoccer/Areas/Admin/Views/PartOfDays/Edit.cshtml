@model BITSoccer.Models.PartOfDay

@{
    ViewBag.Title = "Edit";
    Layout = "~/Views/Shared/_LayoutAdminControl.cshtml";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>PartOfDay</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.HiddenFor(model => model.PartOfDayID)

        <div class="form-group">
            @Html.LabelFor(model => model.PartOfDay1, htmlAttributes: new { @class = "control-label col-md-2" })
            <div class="col-md-10">
                @Html.EditorFor(model => model.PartOfDay1, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.PartOfDay1, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="container-fluid">
                <div class="row">
                    <div class="col-md-10 d-flex justify-content-end">
                        <a href="@Url.Action("Index")" class="btn btn-secondary">Quay lại</a>
                        <input type="submit" value="Save" class="btn btn-primary ml-3" />
                    </div>
                </div>
            </div>
        </div>
    </div>
}
